# ✅ Финальная настройка Render (Node определен правильно!)

## Отлично! Render определил Node автоматически! 🎉

Теперь нужно заполнить оставшиеся поля:

## Шаг 1: Прокрутите вниз в форме

Найдите следующие поля (они ниже на странице):

### Root Directory (ВАЖНО!)

- Найдите поле **"Root Directory (Optional)"**
- Введите: `metasiberian-agent`
- ⚠️ Это критически важно!

### Build Command

- Найдите поле **"Build Command"**
- Введите:
  ```
  cd metasiberian-agent && bun install && bun run build
  ```

### Start Command

- Найдите поле **"Start Command"**
- Введите:
  ```
  cd metasiberian-agent && bun run start
  ```

## Шаг 2: Переменные окружения

Прокрутите до секции **"Environment Variables"** и добавьте:

1. Нажмите **"+ Add Environment Variable"**
2. Добавьте по одной:

   - **Key**: `OPENAI_API_KEY`
     **Value**: `sk-dVAfNONRGf76I6PgCf4236B378E84c7dAcE993476509899d`

   - **Key**: `NODE_ENV`
     **Value**: `production`

   - **Key**: `PORT`
     **Value**: `3000`

   - **Key**: `SERVER_PORT`
     **Value**: `3000`

## Шаг 3: Instance Type

- Убедитесь что выбран **"Free"** (не Starter!)
- Внизу справа должно быть "Free $0/month"

## Шаг 4: Деплой!

1. Прокрутите в самый низ формы
2. Нажмите **"Deploy Web Service"** (фиолетовая кнопка)
3. Render начнет деплой!

## Шаг 5: Следите за логами

1. После нажатия "Deploy Web Service" вы перейдете на страницу сервиса
2. Перейдите в раздел **"Logs"** (в левом меню)
3. Смотрите процесс сборки в реальном времени

## ✅ После успешного деплоя

Render предоставит URL вида:
`https://elizaos-xxxx.onrender.com`

Проверьте:
- API: `https://elizaos-xxxx.onrender.com/api/server/ping`
- Web UI: `https://elizaos-xxxx.onrender.com`

## ⚠️ Важно проверить:

- ✅ Language: **Node** (уже правильно!)
- ✅ Root Directory: **metasiberian-agent** (нужно заполнить!)
- ✅ Build Command: **cd metasiberian-agent && bun install && bun run build**
- ✅ Start Command: **cd metasiberian-agent && bun run start**
- ✅ Instance Type: **Free**

