# 🖥️ Деплой на Render через терминал

Render автоматически деплоит последний коммит из ветки `main` при каждом пуше в GitHub. Вы можете использовать терминал для инициации деплоя.

## 🚀 Быстрый деплой

### Linux/macOS (Bash)

```bash
# Простой деплой с автоматическим сообщением
./scripts/deploy-render.sh

# Деплой с кастомным сообщением коммита
./scripts/deploy-render.sh "Обновление функционала агента"
```

### Windows (PowerShell)

```powershell
# Простой деплой
.\scripts\deploy-render.ps1

# Деплой с кастомным сообщением
.\scripts\deploy-render.ps1 -CommitMessage "Обновление функционала агента"
```

## 📝 Ручной деплой через Git

Если скрипты не работают, вы можете сделать деплой вручную:

```bash
# 1. Убедитесь что все изменения закоммичены
git status

# 2. Добавьте изменения (если есть)
git add .

# 3. Создайте коммит
git commit -m "Deploy to Render"

# 4. Запушьте в GitHub
git push origin main
```

Render автоматически обнаружит новый коммит и начнет деплой.

## ⚙️ Настройка автоматического деплоя

Render по умолчанию настроен на автоматический деплой при каждом пуше в ветку `main`. Это настраивается в Render Dashboard:

1. Откройте ваш сервис в Render Dashboard
2. Перейдите в **Settings** → **Build & Deploy**
3. Убедитесь что **Auto-Deploy** включен
4. Проверьте что **Branch** установлен на `main`

## 🔍 Проверка статуса деплоя

### Через терминал (используя Render API)

Для проверки статуса через терминал нужен Render API Key:

```bash
# Установите API ключ (получите в Render Dashboard → Account Settings → API Keys)
export RENDER_API_KEY="your-api-key"

# Проверьте статус сервиса
curl -H "Authorization: Bearer $RENDER_API_KEY" \
  https://api.render.com/v1/services/[service-id]
```

### Через веб-интерфейс

1. Откройте: https://dashboard.render.com
2. Выберите ваш сервис `metasiberian-agent`
3. Перейдите в раздел **Logs** для просмотра процесса деплоя
4. Проверьте статус в разделе **Events**

## 🛠️ Полезные команды

### Просмотр последних коммитов

```bash
# Показать последние 5 коммитов
git log --oneline -5

# Показать коммиты которые еще не запушены
git log origin/main..HEAD --oneline
```

### Отмена последнего коммита (если еще не запушен)

```bash
# Отменить последний коммит, сохранив изменения
git reset --soft HEAD~1

# Отменить последний коммит, удалив изменения
git reset --hard HEAD~1
```

### Принудительный перезапуск деплоя

Если нужно перезапустить деплой без новых коммитов:

1. Откройте Render Dashboard
2. Перейдите в ваш сервис
3. Нажмите **Manual Deploy** → **Deploy latest commit**

Или через API:

```bash
curl -X POST \
  -H "Authorization: Bearer $RENDER_API_KEY" \
  -H "Content-Type: application/json" \
  https://api.render.com/v1/services/[service-id]/deploys
```

## 📋 Чек-лист перед деплоем

- [ ] Все изменения закоммичены
- [ ] Тесты проходят локально (`bun run test` в `metasiberian-agent`)
- [ ] Проект собирается локально (`cd metasiberian-agent && bun run build`)
- [ ] `render.yaml` обновлен и закоммичен
- [ ] Переменные окружения настроены в Render Dashboard
- [ ] Ветка `main` актуальна

## ⚠️ Важные замечания

1. **Автоматический деплой**: Render автоматически деплоит каждый коммит в `main`
2. **Время деплоя**: Обычно занимает 5-10 минут
3. **Бесплатный план**: Приложение "засыпает" после 15 минут бездействия
4. **Логи**: Всегда проверяйте логи деплоя в Render Dashboard при ошибках

## 🔧 Устранение проблем

### Деплой не запускается автоматически

1. Проверьте что коммит запушен в ветку `main`
2. Убедитесь что Auto-Deploy включен в Render Dashboard
3. Проверьте что сервис не приостановлен

### Ошибки при деплое

См. файл `RENDER_TROUBLESHOOTING.md` для детальных инструкций по устранению проблем.

## 📚 Дополнительные ресурсы

- [Render Documentation](https://render.com/docs)
- [Render API Reference](https://render.com/docs/api)
- [GitHub Actions для Render](https://render.com/docs/github-actions)

