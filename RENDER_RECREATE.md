# 🔄 Пересоздание сервиса на Render (Node вместо Docker)

## Проблема

Render определил проект как Docker и не переопределяет автоматически после удаления Dockerfile.

## Решение: Удалить и создать заново

### Шаг 1: Удалите текущий сервис

1. В Render Dashboard откройте ваш сервис `ElizaOS`
2. Перейдите в **Settings** (в левом меню)
3. Прокрутите в самый низ
4. Найдите секцию **"Delete or suspend"**
5. Нажмите **"Delete Service"**
6. Подтвердите удаление

### Шаг 2: Создайте новый Web Service

1. В Render Dashboard нажмите **"New +"** (вверху справа)
2. Выберите **"Web Service"**
3. Подключите GitHub (если еще не подключен)
4. Выберите репозиторий: `shipilovden/ElizaOS`
5. Нажмите **"Connect"**

### Шаг 3: Настройте новый сервис

**ВАЖНО: Теперь Dockerfile переименован, Render должен определить Node автоматически!**

Заполните форму:

- **Name**: `metasiberian-agent` (или `ElizaOS`)
- **Region**: выберите ближайший
- **Branch**: `main`
- **Root Directory**: ⚠️ **ВАЖНО!** Введите `metasiberian-agent`
- **Runtime**: Должно автоматически определить **"Node"** (не Docker!)
- **Node Version**: `23.x` или `23.3.0`
- **Build Command**: 
  ```
  cd metasiberian-agent && bun install && bun run build
  ```
- **Start Command**: 
  ```
  cd metasiberian-agent && bun run start
  ```
- **Instance Type**: **Free** (не Starter!)

### Шаг 4: Переменные окружения

Прокрутите вниз до **"Environment Variables"** и добавьте:

1. **Key**: `OPENAI_API_KEY`
   **Value**: `sk-dVAfNONRGf76I6PgCf4236B378E84c7dAcE993476509899d`

2. **Key**: `NODE_ENV`
   **Value**: `production`

3. **Key**: `PORT`
   **Value**: `3000`

4. **Key**: `SERVER_PORT`
   **Value**: `3000`

### Шаг 5: Деплой

1. Нажмите **"Create Web Service"**
2. Render начнет деплой с Node (не Docker!)
3. Следите за логами в разделе **"Logs"**

## ✅ После успешного деплоя

Render предоставит URL:
`https://metasiberian-agent.onrender.com` (или похожий)

Проверьте:
- API: `https://metasiberian-agent.onrender.com/api/server/ping`
- Web UI: `https://metasiberian-agent.onrender.com`

## ⚠️ Важно!

- Dockerfile переименован в `Dockerfile.backup`, поэтому Render должен определить Node
- Если все еще показывает Docker - удалите сервис и создайте заново
- Root Directory обязательно: `metasiberian-agent`

