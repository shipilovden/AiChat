# 🚀 Пошаговая инструкция: Деплой на Render.com

## Шаг 1: Откройте Render Dashboard

1. Перейдите на: https://dashboard.render.com
2. Войдите в аккаунт
3. Найдите ваш сервис `ElizaOS` (или создайте новый)

## Шаг 2: Настройте Environment (КРИТИЧЕСКИ ВАЖНО!)

1. Откройте ваш сервис
2. Нажмите **"Settings"** (в левом меню)
3. Прокрутите до секции **"Build & Deploy"**
4. Найдите поле **"Environment"**
5. **ИЗМЕНИТЕ** с `Docker` на **`Node`** ⚠️
6. Установите **Node Version**: `23.x` или `23.3.0`
7. Нажмите **"Save Changes"**

## Шаг 3: Проверьте остальные настройки

В той же секции **"Build & Deploy"** проверьте:

- **Root Directory**: `metasiberian-agent` ⚠️ ВАЖНО!
- **Build Command**: 
  ```
  cd metasiberian-agent && bun install && bun run build
  ```
- **Start Command**: 
  ```
  cd metasiberian-agent && bun run start
  ```

## Шаг 4: Переменные окружения

1. В Settings найдите **"Environment Variables"**
2. Убедитесь что добавлены:
   - `OPENAI_API_KEY` = `sk-dVAfNONRGf76I6PgCf4236B378E84c7dAcE993476509899d`
   - `NODE_ENV` = `production`
   - `PORT` = `3000`
   - `SERVER_PORT` = `3000`

## Шаг 5: Запустите деплой

### Вариант A: Автоматический перезапуск

После сохранения настроек Render автоматически:
- Определит что настройки изменились
- Начнет новый деплой
- Вы увидите процесс в разделе **"Events"** или **"Logs"**

### Вариант B: Ручной перезапуск

Если автоматический не запустился:

1. Перейдите в раздел **"Manual Deploy"**
2. Нажмите **"Deploy latest commit"**
3. Или нажмите **"Clear build cache & deploy"** (если были ошибки)

## Шаг 6: Следите за логами

1. Перейдите в раздел **"Logs"**
2. Смотрите процесс сборки в реальном времени
3. Должно быть:
   - ✅ `bun install` - установка зависимостей
   - ✅ `bun run build` - сборка проекта
   - ✅ `bun run start` - запуск сервера

## ✅ Успешный деплой

После завершения вы увидите:
- ✅ Status: **Live**
- ✅ URL: `https://elizaos-xxxx.onrender.com`

Проверьте:
- API: `https://elizaos-xxxx.onrender.com/api/server/ping`
- Web UI: `https://elizaos-xxxx.onrender.com`

## ❌ Если ошибки

### Ошибка: "Cannot find module"

**Причина**: Render все еще использует Docker

**Решение**: 
1. Убедитесь что Environment = `Node` (не Docker!)
2. Очистите build cache: Settings → Clear build cache
3. Перезапустите деплой

### Ошибка: "bun: command not found"

**Причина**: Bun не установлен

**Решение**: 
1. В Build Command добавьте установку bun:
   ```
   curl -fsSL https://bun.sh/install | bash && cd metasiberian-agent && bun install && bun run build
   ```
2. Или используйте Node вместо Bun (но это сложнее)

### Ошибка: "Port already in use"

**Причина**: Неправильный порт

**Решение**: 
1. Убедитесь что `PORT=3000` в переменных окружения
2. Render автоматически назначает порт через переменную `PORT`

## 🔄 Быстрый перезапуск

Если нужно быстро перезапустить:

1. Render Dashboard → ваш сервис
2. Нажмите **"Manual Deploy"**
3. Выберите **"Deploy latest commit"**

## 📝 Полезные ссылки

- Render Dashboard: https://dashboard.render.com
- Логи: Dashboard → ваш сервис → Logs
- Настройки: Dashboard → ваш сервис → Settings

