# 📋 Настройки деплоя на Render.com

**Дата создания:** 2025-11-26  
**Репозиторий:** https://github.com/shipilovden/AiChat

---

## 📊 Текущие данные существующего деплоя

### Информация о текущем сервисе (для справки)

- **Текущее имя сервиса:** `agentchat` (агентчат)
- **Текущий URL:** `https://elizaos-x14x.onrender.com`
- **Service ID:** `srv-d4ipi415pdvs7385r7fg`
- **Статус:** Активен

### Текущие настройки (из Dashboard)

- **Репозиторий:** `https://github.com/shipilovden/AiChat`
- **Ветка:** `main`
- **Git Credentials:** `dev.shipilov@gmail.com`
- **Регион:** `Oregon (US West)`
- **Тип экземпляра:** `Free` (512 MB)
- **Root Directory:** `metasiberian-agent/`
- **Build Command (текущий):** `metasiberian-agent/ $ bun install && bun build`
- **Start Command (текущий):** `metasiberian-agent/ $ bun run start`
- **Pre-deploy Command:** `metasiberian-agent/ $` (пустой)
- **Runtime:** `Node`
- **Node Version:** `23.x`
- **Автоматическое развертывание:** `Не коммитить` (Do not commit) - ⚠️ нужно изменить на `Автоматически`
- **PR-превью:** `Выключено` (Disabled)

### Переменные окружения (текущие)

```
OPENAI_API_KEY=sk-proj-AIcVdQx68yQmFkvbXkGAmYndNWsdTqQ0JN2JnseUoG1La
NODE_ENV=production
PORT=3000
SERVER_PORT=3000
```

---

## 🔧 Основные настройки сервиса

### Общие настройки (General Settings)

- **Имя сервиса (Name):** `aichat` (рекомендуется) или `agentchat`
  - ⚠️ Имя определяет URL: `https://<имя>.onrender.com`
  - Старый URL был: `https://elizaos-x14x.onrender.com`
  - Новый URL будет: `https://aichat.onrender.com`

- **Регион (Region):** `Oregon (US West)` (или выберите ближайший)
- **Тип экземпляра (Instance Type):** `Free` (512 MB RAM)

---

## 🔗 Настройки репозитория

- **Репозиторий (Repository):** `https://github.com/shipilovden/AiChat`
- **Ветка (Branch):** `main`
- **Git Credentials:** `dev.shipilov@gmail.com`

---

## 📁 Настройки сборки и развертывания

### Корневой каталог (Root Directory)
```
metasiberian-agent
```

### Команда сборки (Build Command)

**Рекомендуемая (с установкой Bun):**
```bash
curl -fsSL https://bun.sh/install | bash && export PATH="$HOME/.bun/bin:$PATH" && bun install && bun run build
```

**В формате Render Dashboard (с префиксом директории):**
```
metasiberian-agent/ $ curl -fsSL https://bun.sh/install | bash && export PATH="$HOME/.bun/bin:$PATH" && bun install && bun run build
```

**Текущая команда (упрощенная, если Bun уже установлен):**
```
metasiberian-agent/ $ bun install && bun build
```

⚠️ **Примечание:** Если Bun уже установлен в окружении Render, можно использовать упрощенную версию. Но для надежности лучше использовать полную команду с установкой Bun.

### Команда запуска (Start Command)

**Рекомендуемая (с установкой PATH для Bun):**
```bash
export PATH="$HOME/.bun/bin:$PATH" && bun run start
```

**В формате Render Dashboard (с префиксом директории):**
```
metasiberian-agent/ $ export PATH="$HOME/.bun/bin:$PATH" && bun run start
```

**Текущая команда (упрощенная, если Bun уже установлен):**
```
metasiberian-agent/ $ bun run start
```

⚠️ **Примечание:** Если Bun уже установлен в окружении Render, можно использовать упрощенную версию. Но для надежности лучше использовать полную команду с установкой PATH.

### Команда предварительного развертывания (Pre-deploy Command)
```
(оставить пустым или не указывать)
```

### Runtime и версия
- **Runtime:** `Node` (НЕ Docker!)
- **Node Version:** `23.x`

---

## 🔐 Переменные окружения (Environment Variables)

Добавьте следующие переменные в разделе **Environment Variables**:

```
OPENAI_API_KEY=sk-proj-AIcVdQx68yQmFkvbXkGAmYndNWsdTqQ0JN2JnseUoG1La
NODE_ENV=production
PORT=3000
SERVER_PORT=3000
```

⚠️ **Важно:** `OPENAI_API_KEY` нужно установить вручную в Dashboard, так как в `render.yaml` он помечен как `sync: false` для безопасности.

---

## 📝 Дополнительные настройки

### Автоматическое развертывание (Automatic Deploys)
- **Рекомендуемая настройка:** `Автоматически` (Automatic) - деплой при каждом пуше в `main`
- **Текущая настройка:** `Не коммитить` (Do not commit) - ⚠️ **НУЖНО ИЗМЕНИТЬ** на `Автоматически`

### Фильтры сборки (Build Filters)
- **Включенные пути:** (не указано, использовать все)
- **Игнорируемые пути:** (не указано)

### Рендеринг поддомена (Render Subdomain)
- **Статус:** `Включено` (Enabled)
- **URL:** `https://<имя-сервиса>.onrender.com`

### PR-превью (PR Previews)
- **Статус:** `Выключено` (Disabled)

---

## 🚀 Пошаговая инструкция для нового деплоя

### Шаг 1: Удаление старого сервиса
1. Откройте Render Dashboard: https://dashboard.render.com
2. Найдите сервис `agentchat` (или `elizaos-x14x`)
3. Перейдите в **Settings** → прокрутите вниз
4. Нажмите **"Delete Service"** и подтвердите удаление

### Шаг 2: Создание нового Web Service
1. В Render Dashboard нажмите **"New +"**
2. Выберите **"Web Service"**
3. Подключите GitHub (если еще не подключен)
4. Выберите репозиторий: `shipilovden/AiChat`
5. Нажмите **"Connect"**

### Шаг 3: Настройка сервиса
Заполните форму:

- **Name:** `aichat` ⚠️ **ВАЖНО!** Это определит URL
- **Region:** `Oregon (US West)` или ближайший
- **Branch:** `main`
- **Root Directory:** `metasiberian-agent` ⚠️ **ВАЖНО!**
- **Runtime:** `Node` (не Docker!)
- **Node Version:** `23.x`
- **Build Command:** 
  ```bash
  curl -fsSL https://bun.sh/install | bash && export PATH="$HOME/.bun/bin:$PATH" && bun install && bun run build
  ```
- **Start Command:** 
  ```bash
  export PATH="$HOME/.bun/bin:$PATH" && bun run start
  ```
- **Instance Type:** `Free` (по умолчанию)

### Шаг 4: Переменные окружения
Прокрутите вниз до **"Environment Variables"** и добавьте:

```
OPENAI_API_KEY=sk-proj-AIcVdQx68yQmFkvbXkGAmYndNWsdTqQ0JN2JnseUoG1La
NODE_ENV=production
PORT=3000
SERVER_PORT=3000
```

### Шаг 5: Создание и деплой
1. Нажмите **"Create Web Service"**
2. Render начнет деплой (займет ~5-10 минут)
3. После завершения вы получите URL: `https://aichat.onrender.com`

### Шаг 6: Проверка работы
После деплоя проверьте:
- **API:** `https://aichat.onrender.com/api/server/ping`
- **Web UI:** `https://aichat.onrender.com`

---

## 📄 Файл render.yaml

Проект использует `render.yaml` для автоматической настройки. Текущая конфигурация:

```yaml
services:
  - type: web
    name: aichat
    env: node
    nodeVersion: 23.x
    rootDir: metasiberian-agent
    buildCommand: |
      curl -fsSL https://bun.sh/install | bash
      export PATH="$HOME/.bun/bin:$PATH"
      bun --version
      bun install
      bun run build
    startCommand: |
      export PATH="$HOME/.bun/bin:$PATH"
      bun run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: SERVER_PORT
        value: 3000
      - key: OPENAI_API_KEY
        sync: false  # Установите вручную в Dashboard
```

⚠️ **Примечание:** Если Render автоматически подхватит настройки из `render.yaml`, некоторые поля могут быть заполнены автоматически. Проверьте и при необходимости скорректируйте в Dashboard.

---

## ✅ Чек-лист перед деплоем

- [ ] Репозиторий подключен: `shipilovden/AiChat`
- [ ] Имя сервиса: `aichat` (для правильного URL)
- [ ] Root Directory: `metasiberian-agent`
- [ ] Runtime: `Node` (не Docker!)
- [ ] Node Version: `23.x`
- [ ] Build Command содержит установку Bun
- [ ] Start Command использует Bun
- [ ] Все переменные окружения добавлены:
  - [ ] `OPENAI_API_KEY`
  - [ ] `NODE_ENV=production`
  - [ ] `PORT=3000`
  - [ ] `SERVER_PORT=3000`
- [ ] Автоматическое развертывание включено

---

## 🔍 Полезные ссылки

- **Render Dashboard:** https://dashboard.render.com
- **Репозиторий:** https://github.com/shipilovden/AiChat
- **Документация Render:** https://render.com/docs
- **Логи деплоя:** Dashboard → ваш сервис → Logs

---

## ⚠️ Важные замечания

### "Засыпание" приложения
На бесплатном плане Render приложение "засыпает" после 15 минут бездействия:
- Первый запрос после сна займет ~30 секунд
- Последующие запросы работают нормально
- Это нормально для тестирования

### Автоматический деплой
- Render автоматически деплоит каждый коммит в `main`
- Время деплоя: обычно 5-10 минут
- Всегда проверяйте логи деплоя в Render Dashboard при ошибках

---

**Последнее обновление:** 2025-11-26

