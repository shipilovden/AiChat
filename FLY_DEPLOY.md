# 🚀 Деплой Metasiberian Agent на Fly.io

## Быстрый старт

### 1. Установите Fly CLI (если еще не установлен)

```bash
# Windows (PowerShell)
iwr https://fly.io/install.ps1 -useb | iex

# Или через winget
winget install flyctl
```

### 2. Авторизуйтесь в Fly.io

```bash
fly auth login
```

### 3. Деплой из Fly.io Dashboard

1. Откройте: https://fly.io/dashboard
2. Нажмите **"New App"** или используйте уже открытую страницу
3. Выберите репозиторий `shipilovden/ElizaOS`
4. App name: `elizaos-j4kkrg` (или любое другое)
5. Region: выберите ближайший (например, `iad` для США)
6. Нажмите **"Deploy"**

### 4. Настройте переменные окружения

После деплоя в Fly.io Dashboard:

1. Откройте ваш app
2. Перейдите в **Settings** → **Secrets**
3. Добавьте секреты:

```
OPENAI_API_KEY=sk-dVAfNONRGf76I6PgCf4236B378E84c7dAcE993476509899d
NODE_ENV=production
SERVER_PORT=3000
```

4. Fly.io автоматически перезапустит приложение

### 5. Настройте Root Directory (важно!)

В Fly.io Dashboard:

1. Settings → **App Configuration**
2. Установите **Working Directory**: `metasiberian-agent`
3. Или используйте команду:

```bash
fly config set --working-dir metasiberian-agent
```

### 6. Проверьте деплой

```bash
# Проверьте статус
fly status

# Посмотрите логи
fly logs

# Откройте приложение
fly open
```

## Альтернатива: Деплой через CLI

Если хотите задеплоить через командную строку:

```bash
cd C:\ElizaOS\metasiberian-agent

# Инициализируйте Fly.io (если еще не сделано)
fly launch --name elizaos-j4kkrg --region iad

# Установите секреты
fly secrets set OPENAI_API_KEY=sk-dVAfNONRGf76I6PgCf4236B378E84c7dAcE993476509899d
fly secrets set NODE_ENV=production
fly secrets set SERVER_PORT=3000

# Задеплойте
fly deploy
```

## ✅ Преимущества Fly.io

- ✅ Поддержка WebSocket (Socket.IO работает)
- ✅ Постоянные соединения
- ✅ Глобальная сеть (быстрая загрузка)
- ✅ Простой деплой из GitHub
- ✅ Бесплатный план доступен

## 🔧 Troubleshooting

### Порт не работает

Убедитесь что в `fly.toml`:
- `internal_port = 3000`
- `PORT=3000` в env
- `SERVER_PORT=3000` в env

### WebSocket не работает

Fly.io поддерживает WebSocket автоматически. Убедитесь что:
- Используете HTTPS URL
- Порт настроен правильно

### Ошибки при деплое

Проверьте логи:
```bash
fly logs
```

## 📝 Полезные команды

```bash
# Статус приложения
fly status

# Логи в реальном времени
fly logs

# Открыть приложение в браузере
fly open

# SSH в контейнер
fly ssh console

# Перезапустить
fly apps restart elizaos-j4kkrg

# Посмотреть конфигурацию
fly config show
```

